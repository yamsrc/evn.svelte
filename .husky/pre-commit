if find node_modules -maxdepth 2 -type l -exec test -d {} \; -print 2>/dev/null | grep -q .; then
  echo "Unlink packages (npm i) before committing."
  exit 1
fi

git diff --cached --name-only --diff-filter=d | xargs -r eslint
