export interface Props {
  /**
   * Show contacts actions
   * @default true
   */
  showContacts?: boolean

  /**
   * Show start adventure action
   * @default false
   */
  showStartAdventure?: boolean

  /**
   * Show expenses templates
   * @default false
   */
  showExpensesTemplates?: boolean
}
