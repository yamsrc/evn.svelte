import Connected from './Connected.svelte'
import Transferred from './Transferred.svelte'
import Unchained from './Unchained.svelte'

export const contacts = {
  connected: Connected,
  unchained: Unchained,
  transferred: Transferred,
}
