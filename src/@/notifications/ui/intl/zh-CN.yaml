groups:
  joined:
    me: |-
      !js
      (groupName) => `您已加入${groupName}`
    others: >-
      !js

      (names, groupName) => `${new Intl.ListFormat("zh-CN", { style: "long",
      type: "conjunction" }).format(names)}已加入${groupName}`
accounts:
  created:
    welcome: 欢迎来到 Evnly！
    hint: 滑动以移除
erase: |-
  !js
  (n) => `全部清除 (${n})`
