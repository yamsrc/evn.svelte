groups:
  joined:
    me: |-
      !js
      (groupName) => `Umejiunga na ${groupName}`
    others: >-
      !js

      (names, groupName) => `${new Intl.ListFormat("sw", { style: "long", type:
      "conjunction" }).format(names)} ame${names.length === 1 ? "ji" : "ji"}unga
      na ${groupName}`
accounts:
  created:
    welcome: Karibu kwenye Evnly!
    hint: Sogeza kushoto kuondoa
erase: Futa zote
