context: Mount 'notifications' context
inputs:
  groups:
    joined:
      me:
        input: You have joined the group {name}
        context: Make it function
      others:
        input: joined the group {name}
        context: Make it function
  accounts:
    created:
      hint:
        input: swipe to remove
  erase:
    input: Erase all (${n})
    context: Make it function
