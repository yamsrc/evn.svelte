context: Mount 'notifications' context
inputs:
  groups:
    joined:
      me:
        input: You have joined {groupName}
        context: make it a function
      others:
        input: "{...names} joined {groupName}"
        context: make a function, that accepts array of names and a group name. list is formatted using Intl.ListFormat
  accounts:
    created:
      hint:
        input: swipe to remove
  erase:
    input: Erase all (${n})
    context: Make it function
